import React from "react";
import PropTypes from "prop-types";

export const Colors = () => {
  

  return (
    <div>
      <span></span>
    </div>
  );
};

Colors.propTypes = {
  ColorsName: PropTypes.string.isRequired,
  
};
